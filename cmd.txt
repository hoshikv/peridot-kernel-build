DMSM="$GITHUB_WORKSPACE/display-drivers/vendor_opensource_display-drivers-peridot-u-oss/msm"
echo "=== internals.h locations ==="
find "$DMSM" -name "internals.h" 2>/dev/null
echo "=== include dirs referenced in mi_dsi_panel.c ==="
head -30 "$DMSM/mi_disp/mi_dsi_panel.c" | grep -n "include" | head
echo "=== dsi dir listing ==="
ls "$DMSM/dsi" 2>/dev/null | head -30
echo "=== sde dir has internals? ==="
find "$DMSM" -name "*.h" -path "*internals*" 2>/dev/null | head
