export PATH="$HOME/tc/bin:$PATH"
D="$GITHUB_WORKSPACE/display-drivers/vendor_opensource_display-drivers-peridot-u-oss/msm"
echo "ko: $(ls -la "$D/msm_drm.ko" | awk '{print $5" "$6" "$7" "$8}')"
echo "gh: $(which gh || echo none)"
echo "token: $( [ -n "$GITHUB_TOKEN" ] && echo set || echo none)"
echo "cache: $( [ -n "$ACTIONS_RUNTIME_TOKEN" ] && echo set || echo none)"
