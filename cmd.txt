M="$GITHUB_WORKSPACE/modules/qcom/opensource"
echo "=== display-drivers/hdcp ls ==="
ls "$M/display-drivers/hdcp"
echo "=== hdcp Kbuild ==="
head -50 "$M/display-drivers/hdcp/Kbuild" 2>/dev/null || echo "NO Kbuild in hdcp"
echo "=== config conf for companions ==="
for d in "display-drivers" "mmrm-driver" "mm-drivers/hw_fence" "mm-drivers/sync_fence" "mm-drivers/msm_ext_display"; do
  echo "-- $d/config --"
  ls "$M/$d/config" 2>/dev/null | head
done
