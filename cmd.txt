set -e
cd "$GITHUB_WORKSPACE"
echo "=== disk ==="
df -h / | tail -1
echo "=== workspace ==="
ls -la
echo "=== kernel ==="
ls kernel/Makefile 2>/dev/null && echo "kernel OK" || echo "kernel MISSING"
ls kernel/.git 2>/dev/null && echo "kernel git OK" || echo "kernel git MISSING"
echo "=== display-drivers (in kernel tree, port-doze2) ==="
ls kernel/qcom/opensource/display-drivers/msm/Kbuild 2>/dev/null && echo "dd OK" || echo "dd MISSING"
echo "=== clang ==="
ls toolchain/clang-r530567/bin/clang 2>/dev/null && echo "clang OK" || echo "clang MISSING"
echo "=== out ==="
ls out/ 2>/dev/null | head -5 || echo "out MISSING"
echo "=== done ==="
