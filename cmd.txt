cd "$GITHUB_WORKSPACE/kernel"
make mrproper 2>&1 | tail -3
echo "=== clean check ==="
ls .config 2>&1 || echo "no .config"
O="$GITHUB_WORKSPACE/out"
scripts/kconfig/merge_config.sh -m -r \
  arch/arm64/configs/gki_defconfig \
  arch/arm64/configs/vendor/pineapple_GKI.config \
  arch/arm64/configs/vendor/peridot_GKI.config > "$O/merge.log" 2>&1
cp .config "$O/.config"
echo "=== olddefconfig ==="
make O="$O" olddefconfig 2>&1 | tail -5
grep -E "CONFIG_DRM_MSM|MI_DISPLAY|CONFIG_MI_DUMP_DISPLAY" "$O/.config"
df -h / | tail -1
