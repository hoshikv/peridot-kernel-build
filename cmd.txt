export PATH="$HOME/tc/bin:$PATH"
M="$GITHUB_WORKSPACE/modules/qcom/opensource"
MMD="$M/mm-drivers"
for f in "$MMD/sync_fence/Module.symvers" "$MMD/hw_fence/Module.symvers" "$MMD/msm_ext_display/Module.symvers" "$M/mmrm-driver/Module.symvers" "$M/securemsm-kernel/Module.symvers"; do
  if [ -f "$f" ]; then echo "OK $f ($(wc -c < "$f")B)"; else echo "MISSING $f"; fi
done
df -h / | tail -1
