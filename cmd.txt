M="$GITHUB_WORKSPACE/modules/qcom/opensource"
echo "===== securemsm hdcp Kbuild ====="
find "$M/securemsm-kernel/hdcp" -maxdepth 1 -type f | head
head -50 "$M/securemsm-kernel/hdcp/Kbuild" 2>/dev/null || echo "NO Kbuild"
echo "===== hw_fence full Kbuild ====="
cat "$M/mm-drivers/hw_fence/Kbuild"
echo "===== conf files ====="
ls "$M/mm-drivers/hw_fence/config" "$M/mm-drivers/config" 2>/dev/null
echo "===== gki conf for pineapple in mm-drivers ====="
find "$M/mm-drivers" -name "*pineappl*" | head
