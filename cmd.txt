export PATH="$HOME/tc/bin:$PATH"
D="$GITHUB_WORKSPACE/display-drivers/vendor_opensource_display-drivers-peridot-u-oss/msm"
K="$GITHUB_WORKSPACE/kernel"
O="$GITHUB_WORKSPACE/out"
echo "=== vermagic ==="
llvm-nm "$D/msm_drm.ko" 2>/dev/null | grep -m1 "vermagic" || strings "$D/msm_drm.ko" | grep -m1 "6.1.174"
echo "kernel release: $(cat $O/include/config/kernel.release)"
echo "=== build-id ==="
llvm-readelf -n "$D/msm_drm.ko" 2>/dev/null | grep -m1 "Build ID"
echo "=== symbol present ==="
llvm-nm "$D/msm_drm.ko" 2>/dev/null | grep -m1 "is_aod_and_panel_initialized"
echo "=== FOD HBM enum strings survived ==="
strings "$D/msm_drm.ko" | grep -m2 "LOCAL_HBM_HLPM_WHITE_1000NIT\|LOCAL_HBM_HLPM_WHITE_110NIT"
