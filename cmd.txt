M="$GITHUB_WORKSPACE/modules/qcom/opensource"
O="$GITHUB_WORKSPACE/out"
echo "=== synx-kernel present? ==="
ls "$M/../.." 2>/dev/null
ls "$GITHUB_WORKSPACE/modules" | head -30
ls "$M"
echo "=== kalamammdrivers.conf ==="
cat "$M/mm-drivers/config/kalamammdrivers.conf"
echo "=== MMRM config in .config ==="
grep -E "CONFIG_MSM_MMRM|CONFIG_QTI_HW_FENCE|CONFIG_QCOM_SPEC_SYNC|CONFIG_HDCP_QSEECOM" "$O/.config" | head
echo "=== does mmrm have own conf for pineapple? ls config ==="
ls "$M/mmrm-driver/config"
