echo "=== all internals.h in workspace ==="
find "$GITHUB_WORKSPACE" -name "internals.h" 2>/dev/null | head
echo "=== check if it's in kernel tree ==="
find "$GITHUB_WORKSPACE/kernel" -name "internals.h" 2>/dev/null | head
echo "=== what gitlink/HEAD for display ==="
cd "$GITHUB_WORKSPACE/display-drivers" && git log -1 --oneline
cd "$GITHUB_WORKSPACE/display-drivers/vendor_opensource_display-drivers-peridot-u-oss/msm"
echo "=== grep includes of internals.h ==="
grep -rn "internals.h" "$GITHUB_WORKSPACE/display-drivers" 2>/dev/null | head
