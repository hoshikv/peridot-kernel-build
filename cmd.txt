F="$GITHUB_WORKSPACE/display-drivers/vendor_opensource_display-drivers-peridot-u-oss/msm/mi_disp/mi_dsi_panel.c"
echo "=== test relative paths ==="
cd "$(dirname "$F")"
for p in ../../../../kernel/kernel/irq/internals.h ../../../kernel/kernel/irq/internals.h ../../../../kernel/irq/internals.h; do
  if [ -f "$p" ]; then echo "OK: $p"; else echo "no: $p"; fi
done
echo "=== old line ==="
sed -n "21p" "$F"