M="$GITHUB_WORKSPACE/modules/qcom/opensource"
echo "=== disk ==="
df -h / | tail -1
echo "=== hdcp Kbuild head ==="
head -30 "$M/display-drivers/hdcp/Kbuild" 2>/dev/null
echo "=== mmrm Kbuild ==="
ls "$M/mmrm-driver"
head -20 "$M/mmrm-driver/Kbuild" 2>/dev/null
echo "=== hw_fence/sync_fence/ext_display Kbuild ==="
for d in hw_fence sync_fence msm_ext_display; do echo "--$d--"; ls "$M/mm-drivers/$d"; head -15 "$M/mm-drivers/$d/Kbuild" 2>/dev/null; done
