M="$GITHUB_WORKSPACE/modules/qcom/opensource/securemsm-kernel"
echo "=== .ko produced ==="
find "$M" -name "*.ko" | head
echo "=== symvers content ==="
cat "$M/Module.symvers"
echo "=== undefined UND list in hdcp module if exists ==="
nm -u "$M/hdcp_qseecom_dlkm.ko" 2>/dev/null | head -20
