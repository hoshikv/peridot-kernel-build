M="$GITHUB_WORKSPACE/modules/qcom/opensource"
K="$GITHUB_WORKSPACE/kernel"
echo "=== how is securemsm hdcp built? Android.bp/Makefile? ==="
find "$M/securemsm-kernel" -maxdepth 2 -name "Android.bp" -o -maxdepth 2 -name "Android.mk" -o -maxdepth 2 -name "Kbuild" | head
ls "$M/securemsm-kernel"
echo "=== mmrm Kbuild ==="
cat "$M/mmrm-driver/Kbuild" | head -60
ls "$M/mmrm-driver"
echo "=== sync_fence Kbuild ==="
cat "$M/mm-drivers/sync_fence/Kbuild"
echo "=== ext_display Kbuild ==="
cat "$M/mm-drivers/msm_ext_display/Kbuild" | head -50
